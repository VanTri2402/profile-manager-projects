import axios from "axios";
import { useDispatch } from "react-redux";

const API_URL = process.env.REACT_APP_API_URL || "http://localhost:5000/api";

const dispatch = useDispatch;
export const updateStreak = async (user, accessToken) => {};
